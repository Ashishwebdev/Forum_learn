<% @posts.each do |post| %>
    <h1><%= link_to post.title, post %></h1>
    published at
    <%= time_ago_in_words(post.created_at) %>
    by
    <%= post.user.email %>
<% end %>
<br>
<%= link_to 'New Post', new_post_path %>